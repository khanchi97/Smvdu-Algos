#include<stdio.h>

int main()
{
    int size;
    scanf("%d",&size);
    int arr[size];
    s_sort(size,arr);

    //print array
    return 0;
}

void s_sort(int size ,int *array)
{
    //complete this function
}
